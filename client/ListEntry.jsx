import React from "react";

//functional compentn
const ListEntry = props => <div>onClick={props.listItem}
  <input type="submit" value="delete" onClick={()=></div>;

//click event, on list . jsx , notfies the list that it was clciked

export default ListEntry;
